# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
build/
dist/
.pytest_cache/

# Mileage local state
mileage.db
*.db
*.db-wal
*.db-shm
*.db-journal

# FUSE leftovers from deleted-but-open files
.fuse_hidden*

# Discovery intake cache — generated by `mileage discover`, not source.
mileage/knowledge/discovered_charts.json
mileage/knowledge/daily_scrape.json

# Live-target diagnostic dumps (scripts/dump_scrape_target_shapes.py) — scratch
# debugging output, not source.
_debug_dumps/

# Env / secrets
.env

# Personal planning / discovery docs (Cursor agent briefs, architecture notes)
Cursor-*.md
Mileage-Plan.md
mileage-ui-mockup.html

# Cursor agent skills (local dev tooling, not runtime)
.agents/
skills-lock.json
