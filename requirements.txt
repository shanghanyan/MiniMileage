# Mileage — Phase 0–5 core dependencies.
# Optional extras live in pyproject.toml: [aggregator], [multiuser] (Redis),
# and [observability] (Arize AX / OpenInference tracing — Phase 5). Each
# degrades gracefully to a no-op when its extra is not installed.
PyYAML>=6.0,<7.0
networkx>=3.0,<4.0
httpx>=0.27,<1.0
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
python-dotenv>=1.0,<2.0
